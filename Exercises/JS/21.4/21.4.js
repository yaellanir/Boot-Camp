const img = document.querySelectorAll("img");

img.addEventListener("click", function (e) {
  // e.target.classList.toggle("on")
  e.target.classList.contains("on");
});

img[0] = "off";

function turnOnOff(params) {
  if (img[0] === "off") {
    e.target.contains.img[1] === "on";
  }
}
